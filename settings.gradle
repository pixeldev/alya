rootProject.name = 'alya'

['bukkit', 'bukkit-abstraction', 'jdk'].forEach {
	include it
}

['v1_8_R3', 'v1_16_R3'].forEach {
	include "versions:${it}"
}

['universal', 'gson'].forEach {
	include "storage:${it}"
}
include 'api'
include 'bungeecord'

